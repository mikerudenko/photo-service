import { makeStyles } from '@material-ui/core/styles';

export const useAdminProfileStyles = makeStyles({
  container: {
    paddingTop: 50,
    paddingBottom: 50,
    display: 'flex',
    flexDirection: 'column',
    flexGrow: 1,
  },
});
