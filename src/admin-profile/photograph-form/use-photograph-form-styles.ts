import { makeStyles } from '@material-ui/core/styles';

export const usePhotographFormStyles = makeStyles({
  form: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    maxWidth: '80%',
    margin: '0 auto',
  },
  photoRow: {
    alignItems: 'center',
  },
});
