import { makeStyles, Theme, createStyles } from '@material-ui/core/styles';

export const useAppRemoveButtonStyles = makeStyles((theme: Theme) =>
  createStyles({
    icon: {
      color: 'rgb(220, 0, 78)',
    },
  }),
);
