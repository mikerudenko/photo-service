import { makeStyles } from '@material-ui/styles';

export const useAppPhoneFieldStyles = makeStyles({
  input: {
    width: '100%',
  },
  wrapper: {
    width: '100%',
    marginBottom: 10,
    height: 80,
  },
});
