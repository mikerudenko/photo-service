import { makeStyles } from '@material-ui/core/styles';

export const useAppTableRowStyles = makeStyles({
  root: {
    width: '100%',
  },
  tableWrapper: {
    maxHeight: 407,
    overflow: 'auto',
  },
});
